# learnings.md — project-local collective wisdom  (Otto memory member 6)
#
# Agent-authored lessons for THIS repo. Auto-appended on non-obvious *resolved* issues.
# Read as a filtered, verify-first slice at session start. Curated by `/otto-distill`.
# Committing is human-gated. Cross-project lessons go in otto-dev/learnings/global.md instead.

## Format
# - [YYYY-MM-DD] scope: repo:<name> | stack: <lib@ver|—> | tag: <build|ci|api|test|...>
#   TRIGGER:  <when this applies — drives retrieval>
#   LESSON:   <the durable tip / what to do>
#   EVIDENCE: <observed where; not assumed> · CONFIDENCE: <low|med|high>
#
# Example (commented demo — a real entry drops the leading '#'; keep this section empty of live entries):
# - [2026-06-08] scope: repo:acme | stack: node@20 | tag: ci
#   TRIGGER:  a routed capability's source isn't installed in the host
#   LESSON:   state the substitution (fall to the registry alternate); never silently skip
#   EVIDENCE: observed during setup, not assumed · CONFIDENCE: high

## Lessons
<!-- agents append below this line -->
